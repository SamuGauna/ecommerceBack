{{#if userDto }}
        {{#if (userAuthenticated userDto.user_role)}}
            <div style="text-align: center;">
                <h1 >Bienvenido {{userDto.user_name}}, tu rol es {{userDto.user_role}}</h1>
                <p>Puedes realizar las siguientes acciones</p>
                <a href="http://localhost:8080/handlebars/realTimeProducts">Agregar y eliminar productos</a>
                <a href="http://localhost:8080/api/sessions/signup">Ver los productos hasta el momento</a>
            </div>
        {{else}}
            <h1 >Bienvenido {{userDto.user_name}}, tu rol es {{userDto.user_role}}</h1>
            <a href="http://localhost:8080/api/products">Ver los productos hasta el momento</a>
                    
        {{/if}}
{{else}}
        <p>Inicia sesión para continuar.</p>
{{/if}}
